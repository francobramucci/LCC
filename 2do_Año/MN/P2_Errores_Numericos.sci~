(-b + sqrt(b**2 - 4*a*c))/(2*a)

function r = misraices(p)
    a = coeff(p, 2)
    b = coeff(p, 1)
    c = coeff(p, 0)
    
    if b > 0 then r(1) = (2*c) /(-b - sqrt(b**2 - 4*a*c))) , r(2) = 
    end
endfunction
